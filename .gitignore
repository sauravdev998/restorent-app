# Rust build output
/api/target/

# Node
node_modules/
/web/dist/
/infra/cdk.out/
/infra/dist/
*.tsbuildinfo

# Environment
.env
.env.local
.env.*.local

# Editors and OS
.DS_Store
*.swp

# Logs
*.log
npm-debug.log*
pnpm-debug.log*

# Playwright's own output: traces, screenshots, and reports from a failed
# browser run. Regenerated by the next run, and a trace is megabytes.
/web/test-results/
/web/playwright-report/
/web/blob-report/
# The same, for a run started from the repository root by mistake.
/test-results/

# NOT ignored on purpose, both are committed and CI checks they are fresh:
#   /api/.sqlx/                        the SQLx offline query cache
#   /web/src/shared/api/schema.d.ts    the generated TypeScript API client types
